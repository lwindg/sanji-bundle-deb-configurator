
include debian.conf


deb:
	mkdir -p deb
	mkdir -p data
	cp -a debian deb/
	cp -a debian.conf deb/
	cp -a debian.mk deb/Makefile
	cp -a $(FILES) $(DIRS) deb/
	(cd deb; \
		dpkg-buildpackage -us -uc -rfakeroot;)

clean:
	rm -rf deb

